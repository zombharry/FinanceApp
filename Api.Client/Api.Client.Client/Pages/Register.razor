@page "/register"
@inject Api.Client.Client.Services.IAuthService AuthService
@inject NavigationManager NavigationManager
<h3>Register</h3>

@* @if (ShowErrors) {
    <div class ="alert alert-danger">
        @foreach (var error in Errors)
        {
            <p>@error</p>
        }
    </div>
}
 *@

 <div class="card">
    <div class="card-body">
        <h5 class="card-title">Register details</h5>
        <EditForm Model="registerRequest" OnValidSubmit="HandleRegister">
            <DataAnnotationsValidator/>
            <ValidationSummary/>
            <div class="form-group">
                <label for="username">Username</label>
                <InputText id="username" class="form-control" @bind-Value="registerRequest.Username" />
                <ValidationMessage For="@(() => registerRequest.Username)" />
            </div>
            <div class="form-group">
                <label for="email">Email</label>
                <InputText id="email" class="form-control" @bind-Value="registerRequest.Email" />
                <ValidationMessage For="@(() => registerRequest.Email)" />
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <InputText id="password" class="form-control" @bind-Value="registerRequest.Password" />
                <ValidationMessage For="@(() => registerRequest.Password)" />
                </div>
         </EditForm>
     </div>
</div>

@code {
    private DTOs.RegisterRequest registerRequest = new DTOs.RegisterRequest();
    private bool ShowErrors;
    private string Error = string.Empty;

    private async Task HandleRegister()
    {
        ShowErrors = false;

        // var result = await AuthService.LoginAsync(registerRequest);
        // if (result is not null)
        // {
        //     NavigationManager.NavigateTo("/");
        // }
        // else
        // {
        //     ShowErrors = true;
        //     Error = "Username or Password is incorrect";
        // }
    }
}
